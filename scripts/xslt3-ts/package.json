{
  "name": "xslt3-ts",
  "version": "1.0.0",
  "description": "Typescript command/control for XSLT (and related) filters, pipelines, validators etc.",
  "main": "index.js",
  "scripts": {
    "start": "nodemon src/index.ts",
    "build": "tsc -p .",
    "refresh": "rm -rf ./node_modules ./package-lock.json && npm install"
  },
  "author": "Wendell Piez and NIST OSCAL team",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^14.0.13",
    "nodemon": "^2.0.4",
    "ts-node": "^8.10.2",
    "typescript": "^4.0.2"
  },
  "dependencies": {
    "commander": "^5.1.0",
    "saxon-js": "^2.1",
    "xslt3": "^2.1"
  },
  "nodemonConfig": {
    "watch": "src/**/*.ts",
    "execMap": {
      "ts": "ts-node"
    }
  }
}
